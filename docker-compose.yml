version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=file:./db.sqlite
    volumes:
      - ./db.sqlite:/app/db.sqlite
    restart: unless-stopped